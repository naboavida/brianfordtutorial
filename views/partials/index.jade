p There are {{posts.length}} widgets
div(ng-repeat='post in posts', ng-class='item-list')
	div.item
		h3 {{post.title}}
		div {{post.text}}
		a(href='readPost/{{post.id}}') More
		| -
		a(href='editPost/{{post.id}}') Edit
		| -
		a(href='deletePost/{{post.id}}') Delete